#include <stdio.h>

// global variables
int x; 
int  y;     // assume 4 bytes

int main(void) {
	scanf("%d", &y);

	printf("%d\n", x);

	z  = ((x*x + y*y) - x*y) * 2


	if (a && z % 2 == 0) || x = 100) {
		// do something here
	}

	return 0;
}
